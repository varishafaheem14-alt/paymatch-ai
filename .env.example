# PayMatch AI Environment Configuration (Optional)
# The application works 100% offline with zero API keys using its built-in rule intelligence.
# If you want to enable live generative Q&A with Google Gemini LLM, provide your API key below:

GEMINI_API_KEY=your_gemini_api_key_here
